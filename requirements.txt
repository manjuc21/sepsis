# Core data processing
pandas==2.2.2
numpy==1.26.4
pyarrow==17.0.0

# Models
scikit-learn==1.5.1
xgboost==2.1.0
# No build-variant suffix: plain PyPI wheels for Linux/Windows already bundle
# CUDA, so `pip install -r requirements.txt` picks up GPU support automatically
# on a machine with an NVIDIA driver (see scripts/setup.sh, which detects this).
# The API container forces the lean CPU-only wheel explicitly via
# docker/Dockerfile.api's --extra-index-url, since inference doesn't need a GPU.
torch==2.4.0

# Explainability
shap==0.46.0

# Backend API
fastapi==0.112.0
uvicorn[standard]==0.30.5
pydantic==2.8.2
sqlalchemy==2.0.32
psycopg2-binary==2.9.9
python-dotenv==1.0.1

# Testing
pytest==8.3.2
httpx==0.27.0

# Utility
tqdm==4.66.5
matplotlib==3.9.1
